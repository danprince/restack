to names ( "bob" "alice" "bill" "alyssa" )

names -- add the names to the stack
random -- use one item from the stack
"hello" -- add greeting to top of stack
swap -- swap greeting with name
" " join -- join stack with spaces
print
